## 1. PR 1C-1: Protocol and Persistence Foundation

- [x] 1.1 Define Proposal-specific human decision contracts that accept only APPROVED and REJECTED, preserve immutable Proposal content, and reject mixed or duplicate terminal facts (strategic-proposal-decision: Proposal content remains immutable; Human decision has one durable authority; Human and system terminal dispositions are mutually exclusive).
- [x] 1.2 Define immutable StrategicProposalInvalidatedTick, StrategicProposalAppliedTick, and StrategicProposalRejectedTick contracts with deterministic identities and source bindings; migration-origin InvalidatedTick includes typed origin, Ready/Resume source IDs, and canonical causal time, while AppliedTick includes immutable legacy disposition and source Mission ID/revision without exposing a standalone save path (strategic-proposal-decision: System invalidation has one durable authority; Phase 1B released waits migrate before decision enablement).
- [x] 1.3 Extend StrategicContractCommit with structured Proposal ID/hash, Approval ID, PlannerRequest ID, expected-base, and source Mission ID/revision fields; require ContractCommit and AppliedTick to bind the same Mission while preserving foundation commit idempotency (strategic-contract-activation: ContractCommit uses structured source binding).
- [x] 1.4 Add Proposal decision and terminal Tick data contracts, SQLite Schema, canonical serialization, typed reads, replay import/export, and ordinary persistence support without adding a standalone public terminal-write operation, another Store, Repository, or database authority.
- [x] 1.5 Add optional grouped source_contract_id, source_contract_revision, source_mission_id, and source_mission_revision fields to the existing StoredTask and workflow_tasks representation, including all-null legacy migration, canonical serialization, replay import/export, typed reads, and no second task model or table (research-authority-routing: StoredTask projection has durable Mission provenance).
- [x] 1.6 Add shared ordinary-write, startup, and replay validators for terminal uniqueness, migration-origin source bindings and canonical causal time, complete APPROVED/REJECTED/INVALIDATED evidence, approval_status redundancy, revision continuity, the consistently bound Proposal-derived ACTIVE research Mission with closed set_research semantics, grouped task provenance, legacy execution quiescence, and pre-delete replay failure (strategic-proposal-decision: Phase 1B released waits migrate before decision enablement; strategic-contract-activation: Approval status is a redundant snapshot; Complete approval evidence is required; Replay preflight preserves target data on failure).
- [x] 1.7 Add protocol and persistence-shape tests for immutable Proposal content, unsupported shared Approval decisions, mixed or incomplete terminal facts, duplicate Invalidated Ticks, migration-origin source/time validation, rejected/invalidated no-Contract evidence, ACTIVE research Mission enforcement, closed set_research semantics, all-null legacy task provenance, complete Mission provenance, and rejected partial provenance; PR 1C-1 validates but does not perform a terminal decision transition or change research routing.
- [x] 1.8 Add forged-state database and replay tests for missing Approval, missing ContractCommit, wrong Proposal hash, missing Applied Tick, migration InvalidatedTick before or unequal to its causal frontier, missing migration sources, non-research/non-ACTIVE Mission, action semantic other than set_research, partial or cross-game task provenance, switched authority with surviving legacy execution, and target preservation on failed import.
- [x] 1.9 Verify PR 1C-1 leaves Engine, user entry points, AuthorityScopeSet activation, legacy research writes, task claim/retry/confirmation behavior, and current Phase 1B Proposal wait behavior unchanged.
- [x] 1.10 Reconcile ADR 0005, the MissionGraph SDD, migration plan, and this active OpenSpec Change with the concrete PR 1C-1 protocol; record reviewed deviations without syncing or archiving the Change.

## 2. PR 1C-2: Dormant Atomic Activation and Authority Projection

- [x] 2.1 Implement dedicated BEGIN IMMEDIATE approved, rejected, and invalidated full-aggregate entry points with same-decision idempotency, conflicting-decision rejection, and stale-invalidation behavior; expose no standalone public ApprovalRecord or terminal Tick save method (strategic-proposal-decision: Repeated and conflicting decisions are deterministic).
- [x] 2.2 Implement all-or-nothing approval activation for legacy research execution disposition, ApprovalRecord, ContractCommit, Contract revision, Applied Tick, the Proposal-derived ACTIVE research Mission, research AuthorityScopeSet, Runtime ROUTING, and Human Wait clearance; ContractCommit and AppliedTick bind the same Mission ID/revision (strategic-contract-activation: Approved activation is atomic; Activation appends the expected revision; ContractCommit uses structured source binding).
- [x] 2.3 Derive the approved Contract revision canonically from immutable Proposal content, require scope research and status ACTIVE, derive execution only from the closed research-to-set_research mapping, and prohibit edited approval, automatic rebase, or action selection through desired_outcome/tool strings/free JSON (strategic-contract-activation: Activated content derives from the Proposal).
- [x] 2.4 Implement atomic rejection and stale invalidation transitions that write no Contract revision and do not recall the Provider.
- [x] 2.5 Under the BEGIN IMMEDIATE lock, re-read every legacy research StoredTask, all ActionAttempts, and pending confirmation; atomically cancel safely disposable work and prove execution quiescence before activating MissionGraph ownership, while leaving every non-research scope unchanged (research-authority-routing: Research authority switches atomically; Legacy research execution is quiescent at activation; Non-research scopes remain unchanged).
- [x] 2.6 Close all legacy research DecisionGap, PlanLease, strategy-state, task creation, retry, confirmation, and equivalent mutation paths when persisted AuthorityScopeSet assigns research to MissionGraph (research-authority-routing: Legacy research writes stop after cutover).
- [x] 2.7 Add authoritative routing projection that writes the PR 1C-1 Contract/Mission provenance group, accepts only the consistently bound ACTIVE research Mission, emits only set_research from the closed mapping, and makes every claim/retry/confirmation/recovery transaction revalidate it; stale, legacy, non-research, non-ACTIVE, or wrong-action tasks stay unclaimable, and Proposal application creates no StoredTask (research-authority-routing: Routing consumes active authoritative revisions; StoredTask projection has durable Mission provenance; Proposal application does not create StoredTask).
- [x] 2.8 Enforce exclusive Decision/Activation Tick intervals so ordinary Ticks start only after the dedicated transition completes (research-authority-routing: Dedicated decision transition precedes ordinary work).
- [x] 2.9 Keep approve/reject/invalidate and authority routing behind a dormant feature gate with no production caller (research-authority-routing: Activation remains dormant until final enablement).
- [x] 2.10 Add deterministic concurrency tests for two approvals, approve/reject races, and a READY research task created after preflight but before the approval transaction acquires its lock; add crash injection after legacy disposition, Approval preparation, Contract preparation, and before transition Tick persistence.
- [x] 2.11 Add restart and replay round-trip tests proving one revision, complete evidence, atomic authority, the Proposal-derived ACTIVE research Mission consistently bound through ContractCommit/AppliedTick/routing, set_research-only projection, quiescent legacy execution, complete current task provenance, stale/legacy/wrong-scope/wrong-status/wrong-action claim rejection, no direct StoredTask, non-research isolation, and pre-delete rejection of invalid switched authority.

## 3. PR 1C-3: Runtime Entry and Enablement

- [ ] 3.1 Add explicit user APPROVE/REJECT entry points bound to the persisted Proposal-ready Human Wait; after enablement, reject generic request_human_resume for that wait while preserving supported non-Proposal resume behavior (strategic-proposal-decision: Phase 1B released waits migrate before decision enablement).
- [ ] 3.2 Before enabling decisions, atomically migrate every historical OPEN Proposal: preserve valid unresolved waits; invalidate a pre-Phase 1C WaitResumedTick with PRE_PHASE1C_WAIT_RELEASED unless a stale reason applies; bind Ready/Resume evidence; set both migration Tick times to exactly one microsecond after the persisted causal frontier; and create no Approval, Contract revision, Provider call, MissionGraph authority, or StoredTask.
- [ ] 3.3 Add ordinary/startup/replay and idempotent migration tests for unresolved waits, one or multiple resumed OPEN Proposals, canonical causal time, forged early/different time, missing source, stale-reason precedence, successful reopen/export/import/re-export, post-migration closure of OPEN plus WaitResumedTick, and pre-delete replay safety.
- [ ] 3.4 Integrate the existing WorkflowEngine/Runtime lineage with dedicated decision transitions and authoritative post-activation routing; do not create a second Engine or composition root.
- [ ] 3.5 Remove dormancy only after startup, replay, crash, concurrency, stale-base, protected Tick-ordering, and Phase 1B migration gates pass on Windows and Ubuntu.
- [ ] 3.6 Add end-to-end approval tests from PlannerRequest and final ProviderAttempt through Proposal Ready, explicit decision, atomic Contract activation of the Proposal-derived ACTIVE research Mission, consistent Mission bindings, and later set_research-only Planner/StoredTask projection with complete current provenance.
- [ ] 3.7 Add end-to-end rejection and invalidation tests proving Contract and AuthorityScopeSet remain unchanged, historical Resume is never interpreted as approval, and ordinary routing resumes only after the dedicated transition Tick.
- [ ] 3.8 Verify legacy research writes and provenance-free, stale, non-research, non-ACTIVE, or non-set_research task claims fail after cutover; civic and all other scopes remain unchanged; and no Proposal directly creates StoredTask.
- [ ] 3.9 Update final SDD, ADR, migration, operator, and replay documentation to match the enabled implementation and record any reviewed deviations from this Change.
- [ ] 3.10 Run full repository checks and both reviews, then run OpenSpec verify, sync the three capabilities, and archive activate-research-authority-from-strategic-proposal.
